// Generated by CIRCT firtool-1.140.0
module Queue1_UInt6_1(
  input        clock,
  input        reset,
  input        io_enq_valid,
  input  [5:0] io_enq_bits,
  input        io_deq_ready,
  output       io_deq_valid,
  output [5:0] io_deq_bits
);

  reg  [5:0] ram;
  reg        maybe_full;
  wire       io_deq_valid_0 = io_enq_valid | maybe_full;
  wire       do_enq = ~(~maybe_full & io_deq_ready) & ~maybe_full & io_enq_valid;
  always @(posedge clock or posedge reset) begin
    if (reset)
      maybe_full <= 1'h0;
    else if (do_enq != (maybe_full & io_deq_ready & io_deq_valid_0))
      maybe_full <= do_enq;
  end // always @(posedge, posedge)
  always @(posedge clock) begin
    if (do_enq)
      ram <= io_enq_bits;
  end // always @(posedge)
  `ifdef ENABLE_INITIAL_REG_
    `ifdef FIRRTL_BEFORE_INITIAL
      `FIRRTL_BEFORE_INITIAL
    `endif // FIRRTL_BEFORE_INITIAL
    initial begin
      if (reset)
        maybe_full = 1'h0;
    end // initial
    `ifdef FIRRTL_AFTER_INITIAL
      `FIRRTL_AFTER_INITIAL
    `endif // FIRRTL_AFTER_INITIAL
  `endif // ENABLE_INITIAL_REG_
  assign io_deq_valid = io_deq_valid_0;
  assign io_deq_bits = maybe_full ? ram : io_enq_bits;
endmodule

